# TODO

## 2026-07-14 提交前审阅收尾

- [x] 修复线源求积临时网格分配、Vue2 风频重复上传和计算/显示污染物耦合三个审阅阻断项。
- [x] 补齐多风向 API 权重校验，并限制风频 XLSX 上传大小和底层异常暴露。
- [x] 同步 README、API、架构、工作流、TODO、CHANGELOG 与测试规模。
- [x] 运行完整 `./scripts/verify.sh`，确认提交前验证通过。

## 原 Python 主界面转译差异复核

以下事项已对照 `扩散注释/backend/templates/index.html`、当前 Vue 主控台和 .NET API 复核。按对计算精度和业务使用影响排序。

### 当前状态

- [x] 原 Python 主界面转译差异复核项已处理完毕；当前没有遗留待办。

## Vue2 平台集成交付收尾

### 当前状态

- [x] Vue2/Vue3 点源可见羽流起点偏移已复现：坐标锚点对齐，但点源近源低浓度段被“羽流突出”阈值裁掉，导致首个可见热力图像素距离污染源约 61 px。
- [x] 已在前端显示层修复：不改后端公式和贡献计算，只为点源按本次单风向结果补源头可见羽流段，避免线源显示成中心点扩散。
- [x] 线源公式已明确改为有限长线源积分法（FLSI）：浓度场和受体贡献均按 `C = ∫₀ᴸ q′K(s)ds` 沿完整起终点积分，`lineSegmentLength` 只是兼容字段名并控制 Gauss-Legendre 积分步长，不构造分段点源或面源。
- [x] Vue2 线源显示改为高于热力图的连续双层线带，并对线源结果图做轻微视觉柔化；仅修改地图渲染，不修改浓度数据和贡献计算。
- [x] 已新增 SO2 业务污染物，并完成目录、Excel、单/多风向计算、公式说明和温湿度增强参数验证。
- [x] Vue2 全局模拟已支持通过独立对话框导入 XLSX 风向加权表、预览导入行并确认权重和，逐方位使用平均风速和权重，同时提供 72 方位模板下载与后端数据校验。
- [x] Vue2 主控台工具栏已改为上下两行，并在窄视口行内换行，所有多风向操作无需横向拖动即可访问。
- [x] 重新运行完整 `./scripts/verify.sh`。
- [x] 重新生成完整 Vue2 平台集成包和下载链接：`https://share.niaite.com/f/c8af95414354ba9a5dad4b5e43bb692a`。

### 后续仅在新反馈出现时追加

- 如产品需要逐个污染源查看单源扩散图，需要新增“按污染源查看贡献图层”功能；当前主图展示的是所有参与污染源叠加后的总浓度热力图。

### 已复核但暂不列为缺陷

- 主控台风速/风向改为临时参数，不写回气象场：这是当前产品规则明确要求，属于设计变化，不按缺陷处理。
- 右侧贡献排名强化为“空气站点污染源贡献排名”：符合当前业务目标，保留；展示层级为“空气站点 → 污染物指标 → 污染源”，且不应影响下一次计算污染物筛选。
- QY 与 GNN 当前相关前端文件同步一致：本轮未发现两套代码之间的展示逻辑差异。
